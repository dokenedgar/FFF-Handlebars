{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=html --reporter=text mocha build/test/testAPIs.js --exit",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "build": "rm -rf build && babel ./ -d build --ignore node_modules",
    "start": "npm run build && node build/pgdb/createtables.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ajv": "^6.5.3",
    "chai": "^4.1.2",
    "chai-http": "^4.2.0",
    "coveralls": "^3.0.2",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "mocha": "^5.2.0",
    "nyc": "^13.0.1",
    "pg": "^7.4.3",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "eslint": "^5.6.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0"
  }
}
